@model List<Restaurant_QKA.Models.InventoryTransaction>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/StaffWareHouse/Views/Shared/_LayoutStaffWareHouse.cshtml";
}

<section class="billboard container-fluid">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <!-- Tiêu đề và mô tả -->
                    <div>
                        <h4 class="card-title">Lịch Sử Các Giao Dịch</h4>
                        <p class="card-description text-muted">Tổng quan về các giao dịch nhập kho gần đây</p>
                    </div>
                    <!-- Nút thêm sản phẩm mới -->
                    <div>
                        <button type="button" class="btn btn-warning newMaterialButton" id="newSupplierButton">
                            Tạo mới đơn nhập kho
                        </button>
                    </div>
                </div>

                <!-- Thanh tìm kiếm và chọn danh mục -->
                <div class="d-flex justify-content-between mb-4">
                    <!-- Tìm kiếm sản phẩm -->
                    <div class="search-container">
                        <form class="search-form d-flex align-items-center" method="get" action="@Url.Action("Search","HomeWareHouse")">
                            <input type="search" class="form-control" name="query" placeholder="Nhập từ khóa....." title="Search here">
                        </form>
                    </div>

                </div>
            </div>
            <!-- Bảng danh sách nguyên liệu  -->
            <div class="table-responsive">
                <!-- Tiêu đề bảng -->
                <table class="table table-hover">
                    <thead class="text-center">
                        <tr>
                            <th>Mã Đơn</th>
                            <th>Mã Nguyên Liệu</th>
                            <th>Mã Nhân Viên Nhập Kho</th>
                            <th>Số Lượng Nhập</th>
                            <th>Ngày Nhập</th>
                            <th>Thao Tác</th>
                        </tr>
                    </thead>
                    <!-- Giá trị từng dòng -->
                    <tbody class="text-center">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="max-width: 150px; word-wrap: break-word;">@item.ITID</td>
                                <td style="max-width: 200px; word-wrap: break-word; white-space: normal;">@item.MaterialID</td>
                                <td style="max-width: 200px; word-wrap: break-word; white-space: normal;">@item.StaffID</td>
                                <td style="max-width: 200px; word-wrap: break-word; white-space: normal;">@item.Quantity</td>
                                <td style="max-width: 200px; word-wrap: break-word; white-space: normal;">@item.TransactionDate</td>
                                <!-- Các nút action -->
                                <td>
                                    @Html.ActionLink("Sửa", "Edit", new { id = item.ITID },
                                                                     new
                                                                     {
                                                                         @class = "btn btn-warning editProductButton",
                                                                         @data_id = item.ITID,
                                                                         id = "editProductButton"
                                                                     })
                                    @Html.ActionLink("Xóa", "Delete", new { id = item.ITID },
                                                                         new
                                                                         {
                                                                             @class = "btn btn-danger deleteProductButton",
                                                                             @data_id = item.ITID,
                                                                             id = "deleteProductButton"
                                                                         })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <!-- Phân trang -->
                <!--<div class="pagination-container">-->
                @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page, ViewBag.Categories }))*@
                <!--</div>-->
            </div>
        </div>
    </div>
</section>

